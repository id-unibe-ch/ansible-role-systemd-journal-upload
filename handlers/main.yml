---
# handlers file for systemd-journal-upload

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart systemd-journal-upload.service
  ansible.builtin.systemd:
    name: systemd-journal-upload.service
    state: restarted
